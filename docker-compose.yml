services:
  dot-prompt:
    build:
      context: .
      dockerfile: Dockerfile.headless
    image: dot-prompt:headless
    container_name: dot-prompt-server
    restart: unless-stopped
    ports:
      - "${PROMPT_SERVER_PORT:-4000}:4000"
    volumes:
      - "${PROMPT_DIR_HOST:-./server/prompts}:/app/prompts:ro"
    environment:
      - MIX_ENV=prod
      - PORT=4000
      - PROMPTS_DIR=/app/prompts
      - PHX_HOST=${PHX_HOST:-localhost}
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - LIVE_VIEW_SIGNING_SALT=${LIVE_VIEW_SIGNING_SALT}
      # Remove DISABLE_UI to enable API endpoint
      # - DISABLE_UI=true
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/127.0.0.1/4000'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
